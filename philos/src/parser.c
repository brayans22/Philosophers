#include "../headers/philosopher.h"

void	parser_arguments(char **arguments)
{
    int i;
	int is_valid_args;

    if (!*arguments || !arguments)
		return (ERROR);

    i = 0;
	is_valid_args = is_
	if (!is_v)
		ft_putendl_fd("Error\n Invalid Arguments!\n", 2);
    return (1);
}
